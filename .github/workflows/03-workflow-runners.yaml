name: 03 - Workflow Runners
on: push
jobs:
    ubuntu-echo:
        runs-on: ubuntu-latest
        steps:
            - name: fist step
              run: echo "first step. Triggered by event name -> ${{ github.event_name}}"  